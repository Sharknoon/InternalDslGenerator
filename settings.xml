<settings>
    <servers>
        <server>
            <id>github</id>
            <username>${env.GITHUB_USERNAME}</username>
            <!-- Public read-only package key -->
            <password>${env.GITHUB_TOKEN}</password>
        </server>
    </servers>
</settings>